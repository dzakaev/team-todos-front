const initialState = {
  product: [],
  loading: true,
};

export const todoReducer = (state = initialState, action) => {
  switch (action.type) {
    case "":

    default:
      return state;
  }
};
